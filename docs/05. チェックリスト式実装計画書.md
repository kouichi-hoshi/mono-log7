---
title: 05. チェックリスト式実装計画書
created: 2025-12-05
description: フェーズ別に進捗を管理するためのチェックリスト
---

# チェックリスト式実装計画書

## 運用前提
- 参照必須: `docs/02. 仕様書.md`、`docs/03. 設計書.md`、`docs/04. 作業計画書.md`、`AGENTS.md`。
- 開発方針: TDD（Red→Green→Refactor）を前提に、未実装の依存はスタブ／モックで先行し、接続ポイント（例: `authAdapter` / `postRepository`）で環境変数切替する。本番ではスタブ無効ガードを入れる。
- テスト: UI／ロジックは Jest+RTL、認証／DBは Route Handler + Server Action + Prisma で結合テスト。`NODE_ENV=test` ではスタブを無効化する。

## ステータス記号と更新手順
- 記号: `☐` 未着手 / `△` 進行中 / `☑` 完了 / `✕` 中止
- 更新手順:
  - 着手前に対象行を `△` にし、必要に応じて期日・担当を追記。
  - 作業完了で `☑` にし、完了条件を満たした根拠（テスト名や確認方法）をコメントとして残す。
  - 仕様変更で無効になった場合は `✕` とし、理由をコメントに残す。

## 共通テンプレ
- カラム: 状態 / ID / 概要 / 完了条件 / 担当 / 期日(YYYY-MM-DD または `-`)
- ID命名: `P{フェーズ}-{領域}-{通番2桁}` 例) `P1-UI-01`
- 完了条件にはテスト/確認方法を含める。
- 記入例:

| 状態 | ID | 概要 | 完了条件 | 担当 | 期日 |
| --- | --- | --- | --- | --- | --- |
| ☐ | Pn-XX-01 | サンプル | Red→Green→Refactorを完了し、関連テストがGREEN | - | 2025-12-31 |

## フェーズ別チェックリスト

### フェーズ0
| 状態 | ID | 概要 | 完了条件 | 担当 | 期日 |
| --- | --- | --- | --- | --- | --- |
| ☑ | P0-SET-01 | `AGENTS.md` 設定と遵守導線の確認 | READMEに「開発者向けガイド」を追加しAGENTSの禁止事項/方針を参照導線化 | - | - |
| ☑ | P0-SET-02 | 必読ドキュメントの配置確認 | `docs/01-04` の配置確認済み＋READMEから主要ドキュメントへの導線を追加 | - | - |

### フェーズ1
| 状態 | ID | 概要 | 完了条件 | 担当 | 期日 |
| --- | --- | --- | --- | --- | --- |
| ☐ | P1-ENV-01 | 開発環境セットアップ（Next15/Biome/Prisma等） | 主要パッケージ導入後、lint/test が通ることを確認 | - | - |
| ☐ | P1-UI-01 | 共通UIパーツ(shadcn button/dialog/alert等)整備 | 指定コンポーネントが画面で動作し再利用できる | - | - |
| ☐ | P1-LAY-01 | 未ログイン／ログインの画面分岐 | ミドルウェアでセッション判定し、想定レイアウトを表示 | - | - |
| ☐ | P1-AUTH-01 | 認証UI（モーダル＋Googleボタン、スタブ） | 開閉・押下動作がスタブ接続で成立し、閉じ操作を実装 | - | - |
| ☐ | P1-EDIT-01 | 投稿エディタ基本挙動 | cat初期値memo、クリア、保存/更新、未入力アラート、保存後クリア＋フォーカス＋トースト | - | - |
| ☐ | P1-LIST-01 | 投稿一覧初期ロード（10件・更新日降順） | スピナー演出を伴いロードし、キャッシュを保持 | - | - |
| ☐ | P1-LIST-02 | 並び替えUI（G1:更新/投稿/手動 × G2:昇降） | 各ボタンで並び順が切り替わる | - | - |
| ☐ | P1-LIST-03 | 手動並び替え（上下ボタン/ドラッグ&ドロップ） | 並び順が操作に追従し、md以上でdnd-kitによるD&Dが機能 | - | - |
| ☐ | P1-FLT-01 | 表示切替（すべて/メモ/TODO）クエリ保持 | URLパラメータで切替・ブラウザ戻る/進むで復元できる | - | - |
| ☐ | P1-TRASH-01 | ゴミ箱ビュー枠と選択/復元/削除UI | trash表示でキャッシュ保持し、選択・復元・削除モーダルの枠を用意 | - | - |
| ☐ | P1-ERR-01 | エラーフィードバック/トースト（開発only） | `?errorTest=` で各トースト表示し、表示後にパラメータ除去（本番無効） | - | - |
| ☐ | P1-SPN-01 | スピナー演出（フェード+レイアウトシフト防止） | shadcnスピナーをフェードイン/アウトし高さを保持 | - | - |
| ☐ | P1-ADP-01 | 接続ポイント雛形（authAdapter/postRepository） | 環境変数でスタブ/本番切替。本番でスタブ無効ガードを実装 | - | - |
| ☐ | P1-TST-01 | TDD: 主要UI/ロジックの単体テスト | Red→Green→Refactor完了。エディタ/一覧/分岐のテストがGREEN | - | - |

### フェーズ2
| 状態 | ID | 概要 | 完了条件 | 担当 | 期日 |
| --- | --- | --- | --- | --- | --- |
| ☐ | P2-DB-01 | DB接続（Prisma + 本番想定DB） | 接続設定が有効でCRUDが実行可能、マイグレーション手順を整備 | - | - |
| ☐ | P2-AUTH-01 | Auth.js Route Handlers 本番接続 | Google OAuthでサインイン/アウトし、セッションがUIに反映 | - | - |
| ☐ | P2-TIM-01 | 投稿ソート（投稿日/更新日 × 昇降） | サーバーデータを指定条件でソートしUIが反映 | - | - |
| ☐ | P2-NSC-01 | 無限スクロール（Server Actions + TanStack Query） | バッチ取得・スピナー・全件取得メッセージが動作 | - | - |
| ☐ | P2-ACC-01 | アカウント削除 | ユーザーデータ削除後セッション終了し、完了メッセージ表示 | - | - |
| ☐ | P2-TAG-01 | 投稿タグ基本（追加/削除/ラベル編集） | タグが保存・表示・更新され、初期値nullを保持 | - | - |
| ☐ | P2-APP-01 | 外観変更（ライト/ダーク/システム・文字サイズ） | 切替がUIに反映し、必要なら永続化される | - | - |
| ☐ | P2-TST-01 | 認証/DB/無限スクロール等のテスト拡充 | 設計書テスト戦略の対象がGREEN（統合/E2E含む） | - | - |

### フェーズ3
| 状態 | ID | 概要 | 完了条件 | 担当 | 期日 |
| --- | --- | --- | --- | --- | --- |
| ☐ | P3-SRCH-01 | テキスト検索（タイトル/本文/カテゴリ/タグ） | 入力→検索→一致結果が表示される | - | - |
| ☐ | P3-TAG-02 | カスタムタグ管理 | 追加/編集/削除ができ、削除時に紐づく投稿は「タグ未登録」扱い | - | - |
| ☐ | P3-LIF-01 | ゴミ箱自動削除 | ゴミ箱滞留期限後に自動削除される | - | - |
| ☐ | P3-EXP-01 | 投稿エクスポート検討/実装 | エクスポート方式を決定し、必要な処理/画面を実装 | - | - |

## 依存関係メモ
- フェーズ1の接続ポイント雛形（P1-ADP-01）がフェーズ2以降の本番接続の前提。
- 認証・DB系テスト（P2-TST-01）は P2-AUTH-01 / P2-DB-01 完了後に着手。
- 検索・タグ・自動削除などフェーズ3の機能は、フェーズ2までのCRUD/タグ実装が前提。
